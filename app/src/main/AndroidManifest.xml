<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="io.mdevlab.ocatestapp">

    <application
        android:name=".app.OCA"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".activity.MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activity.AllChaptersActivity"
            android:label="@string/title_all_chapters"
            android:theme="@style/AppTheme.Details" />
        <activity android:name=".activity.ResultsActivity" />
        <activity
            android:name=".activity.TestActivity"
            android:label="@string/title_test" />
        <activity android:name=".activity.ResponseActivity" />
        <activity android:name=".activity.FavoriteQuestionsActivity" />
        <activity
            android:name=".activity.RandomTestActivity"
            android:label="@string/title_random_question"></activity>
    </application>

</manifest>